# DAC Chain

- Web https://inception.dachain.io/
- Twitter https://x.com/dac_chain
- Discord https://discord.com/invite/dacchain
- Explorer https://exptest.dachain.tech/

## ВАЖНО!!! Перед первым запуском софта необходимо:

- убедиться в правильности активации виртуального окружения VENV
- установить библиотеки командой: pip install -r requirements.txt
- расположить файл `accounts.xlsx` должен находиться в папке config/data/...
- расположить файл `.env` должен находиться в папке config/...

## ВАЖНО!!! Все софты в новых проектах постоянно обновляются (бывает, в день по несколько раз)...

## Поэтому перед запуском того, либо иного софта, всегда необходимо делать обновление "Master > Update Project"!

## Если на момент обновления софта у вас уже запущены активности в нём, то обновление к ним не применится и активности нужно будет перезапустить в PowerShell.

## Старт 24.04.26:

- `Faucet.py` — Early Badge + ежедневный фосет на https://inception.dachain.io
  - Получает разовый Early Badge и клеймит токены, кулдаун 8 часов
  - После каждого прогона обновляет `stats.txt` (Rank, Badges, QE, DACC, Streak)
  - Фильтр: пропускает аккаунт, если фосет на кулдауне

- `Transfers.py` — трансферы нативного токена DACC на DAC Testnet
  - Отправляет DACC на случайные EVM-адреса: 2–5 трансферов за прогон, сумма 0.0001–0.1 DACC каждый
  - Газовый резерв: 0.01 DACC — если баланс ниже, аккаунт пропускается
  - Лимит: 50 трансферов на аккаунт (`TRANSFER_FILTER_LIMIT`), после — аккаунт исключается фильтром
  - Трекинг: `transfers_count.txt` (счётчик), `transfers_date.txt` (дата последнего трансфера)

- `RankNFT.py` — минт ранговых NFT на DAC Testnet https://inception.dachain.io/badge
  - Минтит NFT за ранг: всего 13 штук (Cadet → Crown), по мере набора QE
  - Флоу: `claim-signature` API → подпись → `claimRank()` on-chain (контракт `0xB36a...Bd647`)
  - Газовый резерв: 0.15 DACC — если баланс ниже, минт не выполняется
  - Пауза между минтами одного аккаунта: 20–50 секунд
  - Двойная защита от повторного минта:
    - Слой 1 — `rank_nft_minted.txt`: локальный файл с tx-хэшами, проверяется без RPC
    - Слой 2 — `hasMinted(address, rankId)` on-chain: вторичная проверка через блокчейн
  - NFT заминченные вручную через браузер (API возвращает 400) — учитываются и записываются автоматически
  - Трекинг: `rank_nft_status.txt` (последний NFT + счётчик X/13), `rank_nft_minted.txt` (детали каждого минта)

## Обновление 25.04.26:

- `Crates.py` — открытие ящиков (Quantum Crate) на https://inception.dachain.io/badge
  - Каждый ящик стоит 150 QE и даёт случайную награду
  - Дневной лимит: 5 ящиков на аккаунт, сбрасывается в полночь
  - Фильтр: пропускает аккаунт, если на сегодня уже открыто 5 ящиков или не хватает QE
  - Трекинг: `crates_count.txt` (счётчик с датой)

- `Burn.py` — сжигание DACC для получения QE на https://inception.dachain.io/qe-pool
  - Сжигает 10–25% баланса DACC, сумма рассчитывается с человеческим округлением (1–4 знака после запятой)
  - Минимальный баланс для запуска: 1 DACC
  - Разовое действие — повторный запуск аккаунт пропускает
  - Трекинг: `burn_status.txt`

- `Stake.py` — стейкинг DACC на https://inception.dachain.io/qe-pool
  - Стейкает 10–25% баланса DACC, сумма рассчитывается с человеческим округлением (1–4 знака после запятой)
  - Минимальный баланс для запуска: 1 DACC
  - Разовое действие — повторный запуск аккаунт пропускает
  - Трекинг: `stake_status.txt`

- `IncomingTransfers.py` — входящие переводы DACC от других аккаунтов из списка
  - Для каждого аккаунта выбирает 1–2 случайных отправителей из `accounts.xlsx` и отправляет на него небольшую сумму DACC (0.0001–0.01)
  - Минимальный баланс отправителя: 0.05 DACC — если ниже, отправитель пропускается
  - Разовое действие — повторный запуск аккаунт пропускает
  - Трекинг: `incoming_status.txt`

- `Activity.py` — проверка статуса заданий на https://inception.dachain.io/my-activity
  - Читает профиль и показывает в логах, какие задания выполнены, а какие нет — отдельно для вкладок Social и On-Chain
  - Не совершает никаких действий, только отчёт
  - В конце каждого аккаунта выводит общее число событий и баланс QE
